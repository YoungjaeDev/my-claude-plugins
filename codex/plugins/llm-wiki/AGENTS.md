# llm-wiki — Codex package notes

Generated by codex-bridge plugin emit. Source of truth: `my-claude-plugins/plugins/llm-wiki`.

## Skills

This plugin bundles 6 skill(s) under `skills/`, callable in Codex as `$<name>`.

## Hooks (Claude Code only — inert under Codex)

This plugin defines Claude Code hooks. The Codex hook lifecycle is not yet
mapped, so they are documented here only and do nothing under Codex:

- `UserPromptSubmit (1 command)`
- `PostToolUse (1 command)`
- `SessionStart (1 command)`

