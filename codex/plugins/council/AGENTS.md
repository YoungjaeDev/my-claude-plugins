# council — Codex package notes

Generated by codex-bridge plugin emit. Source of truth: `my-claude-plugins/plugins/council`.

## Skills

This plugin bundles 4 skill(s) under `skills/`, callable in Codex as `$<name>` (3 of them are wrapped Claude commands, named `council-<command>`).

