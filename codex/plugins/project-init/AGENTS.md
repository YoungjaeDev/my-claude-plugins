# project-init — Codex package notes

Generated by codex-bridge plugin emit. Source of truth: `my-claude-plugins/plugins/project-init`.

## Skills

This plugin bundles 1 skill(s) under `skills/`, callable in Codex as `$<name>` (1 of them are wrapped Claude commands, named `project-init-<command>`).

